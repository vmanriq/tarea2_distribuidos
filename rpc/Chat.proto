syntax = "proto3";

message Client{
    int32 id = 1;
    string nombre = 2;
}

message Message{
    Client emisor = 1;
    string contenido  = 2;
    string timestamp = 3;
    int32 id = 4;
    Client receptor = 5;
}

message Empty {}

service Chat{
    rpc SendMessage(Message) return (Empty){}
}
